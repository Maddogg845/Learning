# Add all files to Git
git add .

# Commit your changes
git commit -m "Initial commit"

# Add your GitHub repository as the remote
git remote add origin https://github.com/me50/Maddogg845.git

# Rename your current branch to main (if needed)
git branch -M main

# Push your code to GitHub
git push -u origin main
